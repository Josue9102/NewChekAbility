(function (ext) {
    // Extension code will go here
})(ScratchExtensions);
(function (ext) {
    // Define the block
    ext.in_all_sprites = function() {
        // Code to execute in all sprites
    };

    // Register the block, along with its metadata
    ScratchExtensions.register('In All Sprites', {
        blocks: [
            [' ', 'In All Sprites', 'in_all_sprites']
        ]
    });
})(ScratchExtensions);
